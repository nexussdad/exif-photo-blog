# ================================================
# 本地开发环境配置模板
# 复制此文件并重命名为 .env.local
# ================================================

# ================================================
# 数据库配置（本地 PostgreSQL）
# ================================================

# 本地 PostgreSQL（使用 Docker）
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/photo_blog

# 如果使用其他 PostgreSQL，修改为对应连接字符串
# POSTGRES_URL=postgresql://username:password@localhost:5432/database_name

# 禁用 SSL（本地开发使用）
DISABLE_POSTGRES_SSL=1

# ================================================
# 存储配置（本地文件系统）
# ================================================

# 使用本地存储而非云服务
NEXT_PUBLIC_STORAGE_PREFERENCE=local

# 本地存储路径（相对于项目根目录）
NEXT_PUBLIC_LOCAL_STORAGE_PATH=./uploads

# 本地存储访问 URL
NEXT_PUBLIC_LOCAL_STORAGE_URL=http://localhost:3000/api/local-storage

# ================================================
# 认证配置（本地开发）
# ================================================

# 生成一个随机的 Auth Secret（用于 NextAuth.js）
# 可以使用命令：openssl rand -base64 32
AUTH_SECRET=your-random-secret-key-here

# 管理员账户（可以设置为任何值用于测试）
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# ================================================
# 基础站点配置
# ================================================

# 本地开发域名
NEXT_PUBLIC_DOMAIN=localhost:3000

# 站点标题（可选）
NEXT_PUBLIC_META_TITLE=本地照片博客开发
NEXT_PUBLIC_META_DESCRIPTION=本地开发环境的照片博客

# 导航标题（可选）
NEXT_PUBLIC_NAV_TITLE=本地开发
NEXT_PUBLIC_NAV_CAPTION=开发环境

# ================================================
# 可选配置
# ================================================

# 禁用静态优化（开发时更灵活）
# NEXT_PUBLIC_STATICALLY_OPTIMIZE_PHOTOS=0
# NEXT_PUBLIC_STATICALLY_OPTIMIZE_PHOTO_OG_IMAGES=0

# 启用 AI 功能（可选，需要 OpenAI 密钥）
# OPENAI_SECRET_KEY=your-openai-key-here
# AI_TEXT_AUTO_GENERATED_FIELDS=title,tags,semantic

# 其他可选配置
# NEXT_PUBLIC_DEFAULT_THEME=light
# NEXT_PUBLIC_GRID_ASPECT_RATIO=1.5
# NEXT_PUBLIC_SHOW_LARGE_THUMBNAILS=1

# 开发工具
# ADMIN_DEBUG_TOOLS=1